version: "3.9"

services:
  # ========== 数据库 ==========
  postgres:
    image: pgvector/pgvector:pg16
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ai_factory
      POSTGRES_USER: woami
      POSTGRES_PASSWORD: kali
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init_db.sql:/docker-entrypoint-initdb.d/01_init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U woami -d ai_factory"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - ai_factory_net

  # ========== AI Factory API 服务 ==========
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_factory_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - LLM_API_URL=${LLM_API_URL:-http://host.docker.internal:8081}
      - LLM_MODEL_NAME=${LLM_MODEL_NAME:-Qwen3.6-35B-A3B-UD-Q5_K_M}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DB=ai_factory
      - POSTGRES_USER=woami
      - POSTGRES_PASSWORD=kali
    depends_on:
      postgres:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_factory_net

  # ========== 模型容器 (按需启动，使用 profiles="manual") ==========
  llamacpp-coder:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-coder
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8081:8080"
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/Qwen2.5-Coder-32B-Instruct-Q5_K_M.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net

  llamacpp-writing:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-writing
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8082:8080"  # 将容器的8080端口映射到宿主机的8081端口
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/Qwen3.6-27B-Q5_K_M.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net
      
  llamacpp-default:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-default
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8083:8080"  # 将容器的8080端口映射到宿主机的8081端口
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/Qwen3.6-35B-A3B-UD-Q5_K_M.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net
      
  llamacpp-fast:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-fast
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8084:8080"  # 将容器的8080端口映射到宿主机的8081端口
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/Qwen3.5-9B-Q4_K_M.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net   
      
  llamacpp-reasoning:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-reasoning
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8085:8080"  # 将容器的8080端口映射到宿主机的8081端口
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/DeepSeek-R1-Distill-Qwen-32B-Q5_K_M.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net 
      
  llamacpp-deepseek70b:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-deepseek70b
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8086:8080"  # 将容器的8080端口映射到宿主机的8081端口
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/DeepSeek-R1-Distill-Llama-70B-Q5_K_M.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net
      
  llamacpp-qwen122b:
    image: ghcr.io/ggml-org/llama.cpp:server-rocm
    container_name: llamacpp-qwen122b
    profiles: ["manual"]
    restart: unless-stopped
    ports:
      - "8087:8080"  # 将容器的8080端口映射到宿主机的8081端口
    volumes:
      - /home/data/llama-models:/models:ro
      - /home/data/docker/llama-models.ini:/presets/config.ini:ro
    environment:
      - GGML_HIPBLAS=on # 启用 AMD GPU 加速
      - LLAMA_SET_ROWS=1
      - ROCBLAS_USE_HIPBLASLT=1
      - HSA_ENABLE_SDMA=0
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    command: >-
      -m /models/Qwen3.5-122B-A10B-Q4_K_M-00001-of-00003.gguf
      --models-preset /presets/config.ini
      --host 0.0.0.0
      --port 8080
      --flash-attn on
      -ngl -1
      --threads 16
      --threads-batch 32
      --no-mmap
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    networks:
      - ai_factory_net

networks:
  ai_factory_net:
    driver: bridge
 